singularitySolution
{
  if (isOverheadSingularity)
  { 
    - choose Theta_1
    - Theta_2 - 5 use inverse kinematic
  }
  else
  {
    if (isWristSingularity)
    {
      - theta_5 = 0;
      - ...
    }
    else
    {
      use inverse kinematic
    }
  }
}