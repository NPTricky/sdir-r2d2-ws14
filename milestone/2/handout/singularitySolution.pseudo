singularitySolution
{
  if (isOverheadSingularity)
  { 
    - choose Theta_1
    - for Theta_2 - 5 use inverse kinematic
  }
  else
  {
    if (isWristSingularity)
    {
      - theta_5 = 0;
      - calculate median of next step and previous step of theta_4
      - set theta_4 and calculate theta_6 and so on 
    }
    else
    {
      use inverse kinematic
    }
  }
}
